import { AppShell } from '@/components/ui/AppShell';

export default function NewProjectPage() {
  return (
    <AppShell>
      <h1 className="text-2xl">Create project</h1>
      <p>Use the create form on the Projects page for MVP. Template-based creation can be added from saved templates in DB.</p>
    </AppShell>
  );
}
